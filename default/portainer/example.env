# Shared homelab settings
DOMAIN=lab.example
DATA_BASE=/srv/homelab/data
# DNS is set once in the root example.env (shared).
TRAEFIK_GATEWAY=172.19.0.1

PORTAINER_AGENT_SECRET=change-me-long-random-secret
