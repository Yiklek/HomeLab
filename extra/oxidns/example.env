# Directory holding the OxiDNS configuration
# Must contain config.yaml; export the default from the image with:
#   docker run --rm --entrypoint /bin/cat svenshi/oxidns:1.5.2 \
#     /etc/oxidns/config.yaml > config.yaml
OXIDNS_CONFIG_DIR=
